{
  "name": "lost-ui-utils",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:vue3": "pnpm run -C playground/vue3 dev",
    "start:mock": "pnpm run -C playground/vue3 mock",
    "build": "pnpm -r --filter='./packages/*' run build",
    "test": "vitest",
    "test:coverage": "vitest run --coverage --coverage.exclude=playground",
    "release": "bumpp -r --all",
    "publish:release": "pnpm run build && pnpm -r publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sokamoka/lost-ui.git"
  },
  "workspaces": [
    "packages/*",
    "playground/**"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Sokamoka/lost-ui/issues"
  },
  "homepage": "https://github.com/Sokamoka/lost-ui#readme",
  "devDependencies": {
    "@vitest/coverage-v8": "^1.4.0",
    "bumpp": "^9.4.0",
    "vitest": "^1.4.0"
  }
}
