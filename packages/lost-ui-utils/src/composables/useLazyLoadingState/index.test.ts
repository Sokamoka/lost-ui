import { describe, expect, it } from 'vitest'
import { useLazyLoadingState } from '.'

describe('useLazyLoadingState', () => {
  it('should be defined', () => {
    expect(useLazyLoadingState).toBeDefined()
  })
})
